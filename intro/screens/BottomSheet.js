import { Text, StyleSheet, View } from "react-native";

export default function BottomSheet() {
  return (
    <View>
      <Text>BottomSheet: Proximamente...</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
